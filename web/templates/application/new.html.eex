<h2>New application</h2>

<%= render "form.html", changeset: @changeset,
                        jobs: @jobs,
                        students: @students,
                        action: application_path(@conn, :create) %>

<%= link "Back", to: application_path(@conn, :index) %>
