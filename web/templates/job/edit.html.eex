<h2>Edit job</h2>

<%= render "form.html", changeset: @changeset,
                        companies: @companies,
                        action: job_path(@conn, :update, @job) %>

<%= link "Back", to: job_path(@conn, :index) %>
